# Accessibility & Inclusive Learning — Text-to-Speech Assistant

A small Flask app that converts user-provided text or uploaded PDFs into spoken audio. Built for accessibility with a simple, keyboard-friendly UI and options for online/offline TTS.

**Live locally:** `http://127.0.0.1:5000`

**Highlights**
- **Text-to-Speech:** Convert typed text to MP3 audio.
- **PDF processing:** Extract text from uploaded PDFs using `PyPDF2`.
- **TTS engines:** Uses `gTTS` (online) by default; project includes an offline engine wrapper.
- **Accessible UI:** High contrast, clear focus states, ARIA labels, and keyboard navigation.

**Quick Start (Windows PowerShell)**

1. Open PowerShell and change to the project folder:

```powershell
cd accessibility-learning-system
```

2. (Optional) Activate the virtual environment:

```powershell
. .venv/Scripts/Activate.ps1
```

3. Install dependencies:

```powershell
e:/inclusive_learning/.venv/Scripts/python.exe -m pip install -r requirements.txt
# or if venv is activated: python -m pip install -r requirements.txt
```

4. Run the app:

```powershell
e:/inclusive_learning/.venv/Scripts/python.exe app.py
# or: python app.py
```

Open your browser at `http://127.0.0.1:5000`.

**Project Layout**

```
accessibility-learning-system/
├── app.py                 # Flask app entry
├── requirements.txt       # Python dependencies
├── README.md
├── templates/             # Flask HTML templates
├── static/                # CSS + JS
├── uploads/               # Temporary uploaded PDFs
├── audio/                 # Generated MP3 files
└── utils/                 # PDF reader and TTS engine wrapper
```

**How to use**
- Go to the home page, paste or type text, or upload a PDF file.
- Click Convert to generate audio — playback and download links appear on the result page.

**Troubleshooting**
- If the server stops, restart with:

```powershell
cd accessibility-learning-system
e:/inclusive_learning/.venv/Scripts/python.exe app.py
```
- If PDF text extraction fails for scanned/image PDFs, try OCR before uploading (not included).

**Files to know**
- `app.py` — main Flask app ([app.py](accessibility-learning-system/app.py#L1-L200))
- `utils/pdf_reader.py` — PDF text extraction
- `utils/tts_engine.py` — TTS wrapper used to produce MP3 files

**Contributing**
- Open an issue or submit a PR to improve accessibility, add engines, or harden file handling.

**License**
- MIT (or choose a license you prefer)

---
_Generated by the project maintainer tooling. For questions, run the app and test the convert flow locally._
